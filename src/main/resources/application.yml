spring:
  application:
    name: "spring-security-jwt-refresh"
  h2:
    console:
      enabled: true
  datasource:
    username: sa
    password:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none

auth:
  jwt:
    secret_key: "37283246r983Supers2312er76ygSecret232r98gh2f8"
    expiration_time_minutes: 2
    refresh_expiration_time_minutes: 5
